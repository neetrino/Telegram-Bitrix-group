export default function Home() {
  return (
    <p style={{ padding: 16, fontFamily: "system-ui" }}>
      Bitrix24 → Telegram (GramJS) API backend. Use POST /api/bitrix/webhook and GET
      /api/health.
    </p>
  );
}
